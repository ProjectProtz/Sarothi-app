/** Reminders screen placeholder — Task D will replace this content. */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function RemindersScreen() {
  return (
    <PlaceholderScreen
      titleKey="screen.reminders.title"
      detailKey="placeholder.taskd"
      icon="💊"
    />
  );
}
