/**
 * Games module — Task B will create game components here.
 *
 * Suggested structure (Task B):
 *   src/games/
 *     MemoryMatch/       — FR-2: card-flip pairs
 *     RoutineRecall/     — FR-3: sequence-ordering
 *     AttentionFocus/    — FR-4: spot-the-difference / odd-one-out
 *     adaptiveEngine.ts  — FR-5: difficulty rule engine
 *
 * Each game should:
 *   - Import useTranslation from @/lib/i18n for all text
 *   - Import addSession from @/lib/storage to save session results
 *   - Accept a patientId prop to associate sessions correctly
 *   - Call speakText from @/lib/voice for voice guidance (Task C will implement)
 */
